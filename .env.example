# Copy to .env and fill values
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
FROM_EMAIL=sender@example.com
PORT=3000

# Optional: EmailJS (no backend) — create an account at https://www.emailjs.com
# Then set these values and the frontend will use EmailJS to send directly from the browser.
VITE_EMAILJS_SERVICE=service_zh9178e
VITE_EMAILJS_TEMPLATE=template_8junyzz
VITE_EMAILJS_USER=narendrasiri514@gmail.com
VITE_API_URL=http://localhost:3000/api/send
